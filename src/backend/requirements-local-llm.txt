# 내장 CPU LLM(코파일럿 폴백)용 선택 의존성.
#
# 설치하지 않아도 백엔드는 정상 동작하며, 이 경우 코파일럿은 규칙 기반
# 생성기로 응답한다. 자체 LLM 으로 초안을 생성하려면 아래를 설치한 뒤
# 모델 파일(GGUF)을 내려받는다. 자세한 절차는 README.md 참고.
#
#   pip install -r requirements-local-llm.txt \
#       --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
#
# 위 인덱스를 쓰면 CPU 전용 사전 빌드 휠이 설치되어 컴파일이 필요 없다.
llama-cpp-python==0.3.34
